### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


using DyadInterface
using DyadInterface: ODEAlg, DEVerbosity, OptimizationLevel
using ModelingToolkit: SymbolicT, toggle_namespacing
using DyadInterface: AbstractTransientAnalysisSpec, TransientAnalysisSpec
@kwdef mutable struct CarStepTestTransientSpec <: AbstractTransientAnalysisSpec
  name::Symbol = :CarStepTestTransient
  var"alg"::ODEAlg.Type = ODEAlg.Auto()
  var"start"::Float64 = 0
  var"stop"::Float64 = 500
  var"abstol"::Float64 = 0.000001
  var"reltol"::Float64 = 0.000001
  var"saveat"::Float64 = 0
  var"dtmax"::Float64 = 0
  var"tstops"::Array{Float64, 1} = []
  var"automatic_discontinuity_detection"::Bool = false
  var"optimize"::OptimizationLevel.Type = OptimizationLevel.Aggressive()
  var"progress"::Bool = true
  var"respecialize"::Bool = false
  var"verbose"::DEVerbosity.Type = DEVerbosity.Standard()
  var"log_file"::String = ""
  # Cruise torque before the step (holds 90 km/h)
  var"tau_lo"::Float64 = 31.078
  # Cruise torque after the step (holds 110 km/h)
  var"tau_hi"::Float64 = 40.119
  # Initial speed, matching the tau_lo cruise (25 m/s = 90 km/h)
  var"v0"::Float64 = 25.0
  # Time of the torque step
  var"t_step"::Float64 = 50.0
  # Open-loop torque step from one cruise to another, on flat road.
  # 
  # The step test of notebook 01: hold a low cruise torque, then step to a higher one and read
  # K, tau and theta off the speed response. The two torques are the cruise values the notebook
  # derives from the steady-state force balance on paper — `tau_lo` holds 90 km/h, `tau_hi` holds
  # 110 km/h — and are exposed as parameters so the step test literally runs the equation the
  # students just wrote down.
  # 
  # The lag state starts at `tau_lo` and the speed at its matching cruise value, so the car begins
  # in equilibrium. Without that the run would burn hundreds of seconds settling before the step,
  # since the vehicle's own time constant is about 60 s.
  var"model"::Union{Nothing, System} = VehicleSystemsComponents.Lecture1.CarStepTest(; name=:CarStepTest)
end

function DyadInterface.run_analysis(spec::CarStepTestTransientSpec)
  overrides = Dict{SymbolicT, SymbolicT}()
  no_namespace_model = toggle_namespacing(spec.model, false)
  push!(overrides, no_namespace_model.tau_lo => spec.var"tau_lo")
  push!(overrides, no_namespace_model.tau_hi => spec.var"tau_hi")
  push!(overrides, no_namespace_model.v0 => spec.var"v0")
  push!(overrides, no_namespace_model.t_step => spec.var"t_step")
  base_spec = TransientAnalysisSpec(;
    name=:TransientAnalysis, overrides, alg=spec.alg, start=spec.start, stop=spec.stop, abstol=spec.abstol, reltol=spec.reltol, saveat=spec.saveat, dtmax=spec.dtmax, tstops=spec.tstops, automatic_discontinuity_detection=spec.automatic_discontinuity_detection, optimize=spec.optimize, progress=spec.progress, respecialize=spec.respecialize, verbose=spec.verbose, log_file=spec.log_file, model=spec.model
  )
  run_analysis(base_spec)
end

CarStepTestTransient(;kwargs...) = run_analysis(CarStepTestTransientSpec(;kwargs...))
export CarStepTestTransient, CarStepTestTransientSpec
