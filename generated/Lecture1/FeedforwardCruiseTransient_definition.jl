### DO NOT EDIT THIS FILE
### This file is auto-generated by the Dyad command-line compiler.
### If you edit this code it is likely to get overwritten.
### Instead, update the Dyad source code and regenerate this file


using DyadInterface
using DyadInterface: ODEAlg, DEVerbosity, OptimizationLevel
using ModelingToolkit: SymbolicT, toggle_namespacing
using DyadInterface: AbstractTransientAnalysisSpec, TransientAnalysisSpec
@kwdef mutable struct FeedforwardCruiseTransientSpec <: AbstractTransientAnalysisSpec
  name::Symbol = :FeedforwardCruiseTransient
  var"alg"::ODEAlg.Type = ODEAlg.Auto()
  var"start"::Float64 = 0
  var"stop"::Float64 = 400
  var"abstol"::Float64 = 0.000001
  var"reltol"::Float64 = 0.000001
  var"saveat"::Float64 = 0
  var"dtmax"::Float64 = 0
  var"tstops"::Array{Float64, 1} = []
  var"automatic_discontinuity_detection"::Bool = false
  var"optimize"::OptimizationLevel.Type = OptimizationLevel.Aggressive()
  var"progress"::Bool = true
  var"respecialize"::Bool = false
  var"verbose"::DEVerbosity.Type = DEVerbosity.Standard()
  var"log_file"::String = ""
  # Feedforward torque holding the target speed on the nominal car
  var"tau_ff"::Float64 = 31.078
  # Constant road gradient, tan(alpha)
  var"grade"::Float64 = 0.0
  # Vehicle mass
  var"m"::Float64 = 1400.0
  # Aerodynamic drag area (Cd * A)
  var"CdA"::Float64 = 0.63
  # Initial speed (25 m/s = 90 km/h)
  var"v0"::Float64 = 25.0
  # Open-loop feedforward cruise: a fixed torque computed from the nominal force balance.
  # 
  # The command is a constant `tau_ff` — the torque that holds the target speed on the nominal car
  # on flat road, run backwards through the driveline from the steady-state force balance. There is
  # no measurement and no correction: the plant is driven open loop, so the settled speed is exactly
  # right only when the world matches the model the torque was computed for. `grade`, `m` and `CdA`
  # are exposed so notebook 02 can perturb that world and show the open-loop error each perturbation
  # produces.
  # 
  # The lag state is preloaded with `tau_ff` and the speed with `v0`, so the nominal run begins
  # already settled instead of spending the vehicle's ~60 s time constant getting there.
  var"model"::Union{Nothing, System} = VehicleSystemsComponents.Lecture1.FeedforwardCruise(; name=:FeedforwardCruise)
end

function DyadInterface.run_analysis(spec::FeedforwardCruiseTransientSpec)
  overrides = Dict{SymbolicT, SymbolicT}()
  no_namespace_model = toggle_namespacing(spec.model, false)
  push!(overrides, no_namespace_model.tau_ff => spec.var"tau_ff")
  push!(overrides, no_namespace_model.grade => spec.var"grade")
  push!(overrides, no_namespace_model.m => spec.var"m")
  push!(overrides, no_namespace_model.CdA => spec.var"CdA")
  push!(overrides, no_namespace_model.v0 => spec.var"v0")
  base_spec = TransientAnalysisSpec(;
    name=:TransientAnalysis, overrides, alg=spec.alg, start=spec.start, stop=spec.stop, abstol=spec.abstol, reltol=spec.reltol, saveat=spec.saveat, dtmax=spec.dtmax, tstops=spec.tstops, automatic_discontinuity_detection=spec.automatic_discontinuity_detection, optimize=spec.optimize, progress=spec.progress, respecialize=spec.respecialize, verbose=spec.verbose, log_file=spec.log_file, model=spec.model
  )
  run_analysis(base_spec)
end

FeedforwardCruiseTransient(;kwargs...) = run_analysis(FeedforwardCruiseTransientSpec(;kwargs...))
export FeedforwardCruiseTransient, FeedforwardCruiseTransientSpec
