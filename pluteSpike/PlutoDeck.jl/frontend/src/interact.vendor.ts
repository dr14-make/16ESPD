// The interact.js a plot cell imports. See the import map in `index.html`.
//
// Reached as `const { default: interact } = await import(…)`, so the default export is the
// whole of the contract.

import interact from "interactjs"

export default interact
