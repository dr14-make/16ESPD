save-exact=true
# Refuses an install on a Node below `engines`. It does not gate `npm run`, so what keeps the
# unit suite from passing by running nothing is the `node` pin in the repository's mise.toml.
engine-strict=true
